# Build (canonical local tree is `build/`; never commit CMake output)
build/
build-*/
cmake-build-*/
out/
compile_commands.json

# IDE
.idea/
*.xcuserstate
*.xcworkspace/xcuserdata/

# macOS
.DS_Store

# JUCE / plugin binaries
*.vst3
*.component
*.a
*.dylib
*.so

# FetchContent
_deps/

# GSD planning (local only)
.planning/

# Vendored third-party agent framework (local tooling, not project code)
.cursor/

# Source audio corpus — kept locally, too large to track (300 MB+)
Media/
data/raw/
data/model_backup_*/

# Regenerable training metadata (rebuild via training/scripts/build_mel_groove_dataset.py)
data/processed/meta.csv
data/processed/meta_groove.csv

# Claude/GSD worktrees (stale, local only)
.claude/worktrees/

# Python training venv (Phase 9+)
training/.venv/
# GMD + TFDS cache (Phase 17)
training/data/
training/artifacts/
training/**/*.pt
training/**/*.pth

# Doxygen (optional local HTML)
docs/doxygen/html/

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
install_manifest.txt
CTestTestfile.cmake
Testing/

# ── GSD baseline (auto-generated) ──
.gsd
.gsd-id
.mcp.json
.bg-shell/
Thumbs.db
*.swp
*.swo
*~
.vscode/
*.code-workspace
.env
.env.*
!.env.example
node_modules/
.next/
dist/
__pycache__/
*.pyc
.venv/
venv/
target/
vendor/
*.log
coverage/
.cache/
tmp/
training/data/lakh/

# Processed ML datasets (regenerate locally; tensors exceed GitHub 100MB limit)
data/processed/*.npy
data/processed/*.npz

# Phase 0+ scratch (keep committed baseline MIDI/TSV)
.artifacts/*
!.artifacts/baseline/
!.artifacts/baseline/**

# DAW peak files + duplicate root-level sample wavs (canonical copies live under data/raw/)
peaks/
*.reapeaks
/*.wav
/forest.png

# ── GSD baseline (auto-generated) ──
nul
nul.*
con
con.*
prn
prn.*
aux
aux.*
com[1-9]
com[1-9].*
lpt[1-9]
lpt[1-9].*

# Docs to keep local
.MDignore/
