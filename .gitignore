# Build
build/
cmake-build-*/
out/
compile_commands.json
# CMake-built Catch2 binary (do not commit; rebuild with `cmake --build build-release --target MetalAccompanimentTests`)
build-release/MetalAccompanimentTests

# IDE
.idea/
*.xcuserstate
*.xcworkspace/xcuserdata/

# macOS
.DS_Store

# JUCE / plugin binaries
*.vst3
*.component
*.a
*.dylib
*.so

# FetchContent
_deps/

# GSD planning (local only)
.planning/

# Claude/GSD worktrees (stale, local only)
.claude/worktrees/

# Python training venv (Phase 9+)
training/.venv/
# GMD + TFDS cache (Phase 17)
training/data/
training/artifacts/
training/**/*.pt
training/**/*.pth

# Doxygen (optional local HTML — see Doxyfile / doxygen-docs target)
docs/doxygen/html/

# Terraform (Phase 16 — local state backend config + plugin dir)
.terraform/
backend.hcl
infra/.terraform/

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
install_manifest.txt
CTestTestfile.cmake
Testing/

# ── GSD baseline (auto-generated) ──
.gsd
.gsd-id
.mcp.json
.bg-shell/
Thumbs.db
*.swp
*.swo
*~
.vscode/
*.code-workspace
.env
.env.*
!.env.example
node_modules/
.next/
dist/
__pycache__/
*.pyc
.venv/
venv/
target/
vendor/
*.log
coverage/
.cache/
tmp/
training/data/lakh/

# ── GSD baseline (auto-generated) ──
nul
nul.*
con
con.*
prn
prn.*
aux
aux.*
com[1-9]
com[1-9].*
lpt[1-9]
lpt[1-9].*
