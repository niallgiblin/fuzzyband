---
phase: 11-pitch-harmony
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01-PLAN.md
files_modified:
  - src/midi/PatternPlayer.h
  - src/midi/PatternPlayer.cpp
  - src/AccompanimentProcessor.cpp
autonomous: true
requirements:
  - PITCH-01
  - PITCH-03
tags:
  - midi
  - real-time

must_haves:
  truths:
    - "Bass MIDI notes are transposed by (effectivePitchRootMidi - 40) semitones when pitch path is active and confident"
    - "Drum channel 10 events are unchanged"
    - "When pitch confidence is zero or silence policy applies, bass behaves as nominal authored notes (offset 0)"
  artifacts:
    - path: "src/midi/PatternPlayer.h"
      provides: "setter for bass semitone offset or equivalent"
      contains: "Bass"
    - path: "src/midi/PatternPlayer.cpp"
      provides: "offset applied only to kBassChannel"
      contains: "kBassChannel"
  key_links:
    - from: "PatternPlayer::emitEventsForRange"
      to: "bass note numbers"
      via: "note + offset for bass channel"
      pattern: "noteOn"
---

<threat_model>
**Trust boundaries:** MIDI output to host; same as pre–Phase 11.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T11-03 | tampering | MIDI note | accept | Offset is deterministic from internal state; no external MIDI input |
</threat_model>

<objective>
Implement **D-11-03**: transpose **bass** notes at playback time by `round(effectivePitchRootMidi) - 40` semitones when `pitchConfidence` indicates an active pitch path; otherwise offset **0**. Wire **`AccompanimentProcessor::processBlock`** to pass the **post-policy** root from the same block into **`PatternPlayer`** before `process()`. Completes **PITCH-03** and finishes **PITCH-01** as “pitch feeds routing.”

Output: `PatternPlayer` applies offset only to channel `kBassChannel` (2); drums unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-pitch-harmony/11-CONTEXT.md
@.planning/phases/11-pitch-harmony/11-RESEARCH.md
@src/midi/MidiPatternLibrary.cpp
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add bass semitone offset API to PatternPlayer</name>
  <files>src/midi/PatternPlayer.h, src/midi/PatternPlayer.cpp</files>
  <read_first>
    - src/midi/PatternPlayer.h (full file)
    - src/midi/PatternPlayer.cpp — `emitEventsForRange` and `emitForList`
    - .planning/phases/11-pitch-harmony/11-01-PLAN.md (FeatureVector field semantics)
  </read_first>
  <action>
    1. Add private member `int bassSemitoneOffset = 0;` (or `std::atomic<int>` if accessed cross-thread — **prefer plain int** set only from audio thread in same plan).

    2. Add public method `void setBassSemitoneOffset(int semitones);` that clamps semitones to a safe range **[-24, 24]** using `juce::jlimit(-24, 24, semitones)` and stores.

    3. In `emitEventsForRange`, inside `emitForList`, when emitting for **bass** channel, compute `int outNote = juce::jlimit(0, 127, static_cast<int>(ev.note) + (channel == kBassChannel ? bassSemitoneOffset : 0));` — implement equivalent logic: only add offset when `channel == kBassChannel`.

    4. Ensure `reset()` sets `bassSemitoneOffset = 0` OR document that offset persists across reset (prefer **reset to 0** for cold start consistency with D-11-04).
  </action>
  <acceptance_criteria>
    - `grep -n "setBassSemitoneOffset\\|bassSemitoneOffset" src/midi/PatternPlayer.h src/midi/PatternPlayer.cpp` exits 0.
    - `grep -n "kBassChannel" src/midi/PatternPlayer.cpp` shows offset gated on bass channel.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Compute offset from FeatureVector policy output and call PatternPlayer</name>
  <files>src/AccompanimentProcessor.cpp</files>
  <read_first>
    - src/AccompanimentProcessor.cpp — full processBlock
    - src/midi/PatternPlayer.h — new setter
  </read_first>
  <action>
    After `fv.pitchRootMidi` and `fv.pitchConfidence` are assigned (plan 01), before `patternPlayer.setBpm` / `setPatternIndex`:

    - If `fv.pitchConfidence <= 0.0f` (or below `kMinPitchConfidence` if you use the same threshold), call `patternPlayer.setBassSemitoneOffset(0)`.
    - Else compute `int rounded = static_cast<int>(std::lround(fv.pitchRootMidi));` and `int offset = rounded - 40;` then `patternPlayer.setBassSemitoneOffset(offset)`.

    Order: set offset **before** `patternPlayer.process(...)`.
  </action>
  <acceptance_criteria>
    - `grep -n "setBassSemitoneOffset" src/AccompanimentProcessor.cpp` exits 0.
    - `grep -n "pitchRootMidi\\|40" src/AccompanimentProcessor.cpp` includes a line computing offset from 40.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Build</name>
  <files></files>
  <read_first></read_first>
  <action>
    `cmake --build build --config Release` from project root.
  </action>
  <acceptance_criteria>
    - Build completes with exit code 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Unit tests in plan 03 assert transposed bass; quick manual check in DAW optional.
</verification>

<success_criteria>
- PITCH-03 satisfied: bass follows detected root within one MIDI semitone quantization when confidence is high; legacy behavior when confidence is zero.
</success_criteria>
