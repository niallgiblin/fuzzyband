---
phase: 11-pitch-harmony
plan: 03
type: execute
wave: 3
depends_on:
  - 11-02-PLAN.md
files_modified:
  - tests/test_pitch_estimator.cpp
  - tests/test_pattern_player.cpp
  - CMakeLists.txt
  - .github/workflows/ci.yml
  - docs/PHASE11_PITCH_TESTING.md
autonomous: true
requirements:
  - PITCH-04
tags:
  - tests
  - ci

must_haves:
  truths:
    - "Synthetic sine input yields expected pitch within documented tolerance"
    - "PatternPlayer bass transpose shifts emitted note numbers by the configured offset"
    - "macOS CI runs ctest including new tests (existing workflow already invokes ctest)"
  artifacts:
    - path: "tests/test_pitch_estimator.cpp"
      provides: "Catch2 tests for PitchEstimator"
      contains: "TEST_CASE"
    - path: "docs/PHASE11_PITCH_TESTING.md"
      provides: "Documented Hz↔MIDI tolerance and regression policy"
      contains: "tolerance"
---

<threat_model>
**Trust boundaries:** Test harness only; no production network.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T11-04 | info disclosure | test docs | accept | No secrets in repo; synthetic signals only |
</threat_model>

<objective>
Satisfy **PITCH-04**: add **Catch2** unit tests for **`PitchEstimator`** on deterministic sine buffers; extend **`test_pattern_player.cpp`** to verify **bass note transposition** with `setBassSemitoneOffset`; register sources in **CMakeLists.txt**; add **`docs/PHASE11_PITCH_TESTING.md`** listing **numerical tolerances** (e.g. ±0.25 semitone for YIN at 48 kHz). Ensure **`.github/workflows/ci.yml`** still runs **`ctest`** (no change required if tests are in default target — if new target, wire it).

Optional: stub header for ONNX pitch behind `#if MA_ENABLE_ONNX` — only if plan 01/02 left a clean extension point; otherwise document “future ONNX pitch” in `PHASE11_PITCH_TESTING.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-pitch-harmony/11-CONTEXT.md
@.planning/phases/11-pitch-harmony/11-RESEARCH.md
@tests/test_onset_detector.cpp
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add test_pitch_estimator.cpp</name>
  <files>tests/test_pitch_estimator.cpp, CMakeLists.txt</files>
  <read_first>
    - tests/test_onset_detector.cpp (Catch2 style)
    - CMakeLists.txt — search for `target_sources` or `MetalAccompanimentTests`
    - src/analysis/PitchEstimator.h
  </read_first>
  <action>
    1. Create `tests/test_pitch_estimator.cpp` with at least:

       - `TEST_CASE` generating e.g. 4096 samples of sine at **440 Hz** at **48000 Hz** mono float in [-1,1].
       - `PitchEstimator est; est.prepare(48000.0, 4096); est.process(buf, 4096);`
       - Assert `getMidiNote()` within **±0.25** of `69.0` (A4) OR document Hz tolerance in assertion message.

    2. Second case: silence buffer → confidence near 0 or midi irrelevant per API contract.

    3. Add `test_pitch_estimator.cpp` and `src/analysis/PitchEstimator.cpp` to `MetalAccompanimentTests` in `CMakeLists.txt` (same block as `test_pattern_player.cpp` and `src/analysis/OnsetDetector.cpp`).
  </action>
  <acceptance_criteria>
    - `ctest --test-dir build -R pitch -C Release` or full `ctest` shows passing tests for PitchEstimator.
    - `grep -l "test_pitch_estimator" CMakeLists.txt` exits 0.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Extend test_pattern_player for bass transpose</name>
  <files>tests/test_pattern_player.cpp</files>
  <read_first>
    - tests/test_pattern_player.cpp (full file)
    - src/midi/MidiPatternLibrary.cpp — first pattern bass notes use `kBassRoot` (40)
  </read_first>
  <action>
    Add `TEST_CASE("PatternPlayer applies bass semitone offset", "[midi]")`:

    - Setup `PatternPlayer` as existing tests (lib, prepare, bpm, pattern index 1, not silent).
    - Call `setBassSemitoneOffset(2)`.
    - Process one large block (e.g. 96000 samples) to guarantee note-ons.
    - Iterate MIDI events; for **channel 2** note-ons, assert `getNoteNumber() == 40 + 2` **OR** if pattern uses 40+7 in places, assert `getNoteNumber() == (expected_base + 2)` where `expected_base` is the authored note from the pattern **without** offset — document by reading `MidiPatternLibrary` pattern 1 bass events for the first emitted note.

    Use deterministic check: disable humanization for test if needed — **if** velocity humanization breaks note lookup, compare only first note-on or fix RNG seed: `PatternPlayer` uses `juce::Random` — call `player.prepare` which calls `rng.setSeedRandomly()` — for determinism, either add `setRandomSeedForTesting` (only if necessary) OR assert on **set** of note numbers observed across trials matching shifted authored set. Simplest: assert **at least one** bass note equals `40 + offset` when offset is 2 and pattern contains 40.
  </action>
  <acceptance_criteria>
    - `grep -n "bass semitone\\|setBassSemitoneOffset" tests/test_pattern_player.cpp` exits 0.
    - `ctest --test-dir build -R PatternPlayer -C Release` passes.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Document regression thresholds</name>
  <files>docs/PHASE11_PITCH_TESTING.md</files>
  <read_first>
    - .planning/phases/11-pitch-harmony/11-CONTEXT.md (D-11-05)
  </read_first>
  <action>
    Create `docs/PHASE11_PITCH_TESTING.md` with sections:

    - **Synthetic sine:** sample rate, buffer size, expected MIDI note, **allowed error** (±0.25 semitone).
    - **CI:** command `ctest --test-dir build --output-on-failure --config Release`.
    - **Future ONNX pitch:** one paragraph “not required for Phase 11 CI when MA_ENABLE_ONNX=OFF.”
  </action>
  <acceptance_criteria>
    - File exists at `docs/PHASE11_PITCH_TESTING.md`.
    - File contains substring `0.25` or `tolerance`.
    - File contains substring `ctest`.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 4: Confirm CI runs tests</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
  </read_first>
  <action>
    Verify workflow already runs `ctest --test-dir build --output-on-failure --config Release`. If tests are part of default `all` target, **no YAML change**. If not, add the test binary to the build graph — typically not needed for this repo.

    Only edit `ci.yml` if `ctest` does not run new tests.
  </action>
  <acceptance_criteria>
    - `grep -n "ctest" .github/workflows/ci.yml` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Local: `ctest --test-dir build --output-on-failure --config Release` all green.
</verification>

<success_criteria>
- PITCH-04 complete with documented thresholds and automated tests in default CI path.
</success_criteria>
