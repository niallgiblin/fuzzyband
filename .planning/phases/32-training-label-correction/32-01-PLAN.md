---
phase: "32"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - training/build_dataset.py
  - training/tests/test_dataset_build.py
autonomous: true
requirements:
  - LABEL-01

must_haves:
  truths:
    - "build_dataset.py assigns Y by rule oracle + Breakdown heuristic, not quantile bins"
    - "GMD state=3.0 rows are clamped to SOFT (class 1) for oracle lookup, not silently labeled SILENT"
    - "SILENT rows (state 0) are never overridden to Breakdown (D-04)"
    - "Non-SILENT rows with note_density < 2.5 AND bpm < 110 are labeled class 6 (Breakdown)"
    - "_activity_score and _labels_from_train_quantiles removed from build_dataset.py"
    - "Unit tests for _rule_pattern_for_state and _oracle_label pass in the existing test suite"
  artifacts:
    - path: "training/build_dataset.py"
      provides: "GMD dataset build with oracle labels"
      contains: "_rule_pattern_for_state"
    - path: "training/build_dataset.py"
      provides: "GMD dataset build with oracle labels"
      contains: "_oracle_label"
    - path: "training/build_dataset.py"
      provides: "GMD dataset build with oracle labels"
      contains: "_K_SOFT_MID_BPM"
    - path: "training/tests/test_dataset_build.py"
      provides: "Unit tests for oracle function"
      contains: "TestRulePatternForState"
  key_links:
    - from: "training/build_dataset.py main()"
      to: "_oracle_label"
      via: "per-row call replacing _activity_score"
      pattern: "_oracle_label\\(bpm_clamped"
    - from: "_rule_pattern_for_state"
      to: "src/inference/pattern_rules.h"
      via: "mirrored thresholds"
      pattern: "_K_SOFT_MID_BPM\\s*=\\s*120\\.0"
---

<objective>
Replace the quantile-bin label oracle in `training/build_dataset.py` with a Python port of `PatternRules::rulePatternForState` plus a Breakdown class (index 6) heuristic. Add unit tests for the new oracle functions.

Purpose: Makes the pattern model a learned distillation of the rule path instead of an ordinal activity ranker. This is the GMD-side half of LABEL-01. The Lakh-side changes and `merge_datasets.py` label passthrough are Plan 02 (atomic pair with Lakh tensor format change).

Output: Corrected `build_dataset.py` (oracle-labeled Y, no quantile bins) + passing unit tests for the oracle and Breakdown heuristic.
</objective>

<execution_context>
@/Users/ng/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ng/Desktop/fuzzyband/.planning/PROJECT.md
@/Users/ng/Desktop/fuzzyband/.planning/ROADMAP.md
@/Users/ng/Desktop/fuzzyband/.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add rule oracle functions to build_dataset.py and replace quantile-bin labeling</name>

  <read_first>
    - training/build_dataset.py (full file — understand current structure before modifying)
    - src/inference/pattern_rules.h lines 18-50 (exact C++ logic to port; thresholds are ground truth)
    - training/build_dataset.py lines 94-110 (functions to delete: _activity_score, _labels_from_train_quantiles)
    - training/build_dataset.py lines 182-236 (main() data-loading and label generation blocks to replace)
  </read_first>

  <behavior>
    - _rule_pattern_for_state(120.0, 0.0) == 0 (SILENT)
    - _rule_pattern_for_state(100.0, 1.0) == 1 (SOFT, bpm < 120)
    - _rule_pattern_for_state(140.0, 1.0) == 2 (SOFT, 120 <= bpm < 160)
    - _rule_pattern_for_state(170.0, 1.0) == 3 (SOFT, bpm >= 160)
    - _rule_pattern_for_state(140.0, 2.0) == 4 (LOUD, bpm < 160)
    - _rule_pattern_for_state(170.0, 2.0) == 5 (LOUD, bpm >= 160)
    - _rule_pattern_for_state(100.0, 3.0) == 1 (GMD state=3.0 clamped to SOFT, slow bpm -> class 1, not 0)
    - _oracle_label(80.0, 0.0, sparse_events) == 0 (SILENT never overridden, D-04)
    - _oracle_label(80.0, 1.0, sparse_events_dens_lt_2.5) == 6 (Breakdown override, D-03)
    - _oracle_label(140.0, 1.0, dense_events) == 2 (oracle result, no Breakdown override)
  </behavior>

  <files>training/build_dataset.py</files>

  <action>
    Read training/build_dataset.py and src/inference/pattern_rules.h before making any change.

    Step 1 — Add module-level constants and oracle functions. Insert after the `_compute_proxy_row` function (before `_midi_bytes_from_tfds`):

    Constants (underscore-prefixed UPPER_CASE per project naming, mirroring pattern_rules.h lines 18-19):
    - _K_SOFT_MID_BPM = 120.0 with comment: mirrors kSoftMidBpmThreshold from src/inference/pattern_rules.h
    - _K_SOFT_LOUD_BPM = 160.0 with comment: mirrors kSoftLoudBpmThreshold

    Function _rule_pattern_for_state(bpm: float, state_float: float, policy_intensity: float = 0.5) -> int:
    - Docstring references D-06 (policyIntensity=0.5 neutral assumption) and Phase 35 (where adjustedBpm goes into X[0])
    - adj_bpm = bpm + (policy_intensity - 0.5) * 40.0
    - state = min(int(state_float), 2)  — clamps GMD state=3.0 to SOFT(1) per research finding; comment explains why
    - state == 0: return 0
    - state == 1: return 1 if adj_bpm < _K_SOFT_MID_BPM, return 2 if adj_bpm < _K_SOFT_LOUD_BPM, else return 3
    - state == 2: return 4 if adj_bpm < _K_SOFT_LOUD_BPM else return 5
    - return 0 (unreachable fallback)

    Function _oracle_label(bpm: float, state_float: float, events: list[dict]) -> int:
    - Docstring references D-03 (Breakdown override), D-04 (state guard), D-06 (neutral intensity)
    - label = _rule_pattern_for_state(bpm, state_float)
    - Breakdown override guard: if label != 0 (not SILENT):
      - ons = [e for e in events if e.get("type") == "note_on"]
      - dens = len(ons) / _duration_beats(events, bpm)
      - if dens < 2.5 and bpm < 110.0: label = 6
    - return label

    Step 2 — Delete the two quantile-bin functions (per D-01):
    - Remove `_activity_score` function entirely (lines 94-102)
    - Remove `_labels_from_train_quantiles` function entirely (lines 105-110)

    Step 3 — Update main() data-loading loop:
    - Change list declarations from `scores: list[float] = []` to `Y_all_list: list[int] = []`
    - In the per-example loop body, replace:
        scores.append(_activity_score(row, events))
      with:
        bpm_clamped = float(row[0])
        state_float = float(row[4])
        Y_all_list.append(_oracle_label(bpm_clamped, state_float, events))

    Step 4 — Update the label-generation block after GroupShuffleSplit:
    - Remove: scores_all = np.asarray(scores, ...), scores_tr, scores_va, Y_train = _labels_from_train_quantiles(...), Y_val = _labels_from_train_quantiles(...)
    - Replace with:
        Y_all = np.asarray(Y_all_list, dtype=np.int64)
        Y_train = Y_all[tr_idx]
        Y_val = Y_all[va_idx]
    - Keep the y_placeholder line for GroupShuffleSplit API — do NOT remove it. Only the `scores` list and quantile calls are removed.
    - Keep X_train and X_val index operations unchanged.

    Do not change: _compute_proxy_row, _duration_beats, _clamp_bpm, _midi_bytes_from_tfds, _example_id, _example_bpm, normalization blocks, torch.save calls, _write_manifest, or the DATA-06 histogram gate (lines 288-297). Those are unmodified per D-07 and scope constraints.
  </action>

  <verify>
    <automated>cd /Users/ng/Desktop/fuzzyband/training && .venv/bin/python -c "import build_dataset as bd; assert bd._rule_pattern_for_state(120.0, 0.0) == 0; assert bd._rule_pattern_for_state(100.0, 1.0) == 1; assert bd._rule_pattern_for_state(140.0, 1.0) == 2; assert bd._rule_pattern_for_state(170.0, 1.0) == 3; assert bd._rule_pattern_for_state(140.0, 2.0) == 4; assert bd._rule_pattern_for_state(170.0, 2.0) == 5; assert bd._rule_pattern_for_state(100.0, 3.0) == 1, 'GMD state=3.0 must clamp to SOFT'; print('oracle spot checks PASS')"</automated>
  </verify>

  <acceptance_criteria>
    - training/build_dataset.py contains `_K_SOFT_MID_BPM  = 120.0` and `_K_SOFT_LOUD_BPM = 160.0`
    - training/build_dataset.py contains `def _rule_pattern_for_state(`
    - training/build_dataset.py contains `def _oracle_label(`
    - training/build_dataset.py contains `min(int(state_float), 2)` inside _rule_pattern_for_state
    - training/build_dataset.py does NOT contain `_activity_score`
    - training/build_dataset.py does NOT contain `_labels_from_train_quantiles`
    - training/build_dataset.py does NOT contain `scores_all`
    - training/build_dataset.py contains `Y_all_list` in main()
    - training/build_dataset.py contains `y_placeholder` (GroupShuffleSplit API line preserved)
    - Inline verify command exits 0 with "oracle spot checks PASS"
  </acceptance_criteria>

  <done>build_dataset.py generates oracle labels Y∈[0,6] via rule oracle + Breakdown heuristic; quantile-bin functions deleted; GMD state=3.0 clamped correctly; all spot-check assertions pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add oracle unit tests for _rule_pattern_for_state and _oracle_label</name>

  <read_first>
    - training/tests/test_dataset_build.py (full file — understand fixture and class structure before extending)
    - training/tests/test_dataset_build.py lines 27-35 (build_dataset_module fixture to reuse)
  </read_first>

  <behavior>
    - TestRulePatternForState::test_silent_returns_0: _rule_pattern_for_state(120.0, 0.0) == 0
    - TestRulePatternForState::test_soft_slow_returns_1: _rule_pattern_for_state(100.0, 1.0) == 1
    - TestRulePatternForState::test_soft_mid_returns_2: _rule_pattern_for_state(140.0, 1.0) == 2
    - TestRulePatternForState::test_soft_fast_returns_3: _rule_pattern_for_state(170.0, 1.0) == 3
    - TestRulePatternForState::test_loud_mid_returns_4: _rule_pattern_for_state(140.0, 2.0) == 4
    - TestRulePatternForState::test_loud_fast_returns_5: _rule_pattern_for_state(170.0, 2.0) == 5
    - TestRulePatternForState::test_gmd_state3_clamped_to_soft: _rule_pattern_for_state(100.0, 3.0) == 1
    - TestOracleLabel::test_breakdown_override_fires: _oracle_label(80.0, 1.0, 5_sparse_note_on_events) == 6
    - TestOracleLabel::test_silent_never_overridden: _oracle_label(80.0, 0.0, any_sparse_events) == 0
  </behavior>

  <files>training/tests/test_dataset_build.py</files>

  <action>
    Read training/tests/test_dataset_build.py to understand existing class/fixture style before adding. Append two new test classes after the last existing class, using the same `build_dataset_module` fixture.

    Class TestRulePatternForState (7 methods):
    - test_silent_returns_0: assert _rule_pattern_for_state(120.0, 0.0) == 0
    - test_soft_slow_returns_1: assert _rule_pattern_for_state(100.0, 1.0) == 1
    - test_soft_mid_returns_2: assert _rule_pattern_for_state(140.0, 1.0) == 2 (bpm=140, between 120 and 160)
    - test_soft_fast_returns_3: assert _rule_pattern_for_state(170.0, 1.0) == 3 (bpm=170 >= 160)
    - test_loud_mid_returns_4: assert _rule_pattern_for_state(140.0, 2.0) == 4
    - test_loud_fast_returns_5: assert _rule_pattern_for_state(170.0, 2.0) == 5
    - test_gmd_state3_clamped_to_soft: assert _rule_pattern_for_state(100.0, 3.0) == 1 with comment explaining GMD 4-class state

    Class TestOracleLabel (2 methods):
    - test_breakdown_override_fires: construct 5 note_on events at time_sec = 0.0, 1.0, 2.0, 3.0, 4.0, note=36, velocity=64; call _oracle_label(80.0, 1.0, events); assert result == 6. (dens = 5 events / (4.0 * 80/60 beats) = ~0.94 < 2.5, bpm=80 < 110)
    - test_silent_never_overridden: same 5 events but state_float=0.0; assert _oracle_label(80.0, 0.0, events) == 0

    Follow existing naming convention: class names PascalCase, method names snake_case, same (self, build_dataset_module) parameter pattern. No new fixtures needed — reuse existing build_dataset_module.
  </action>

  <verify>
    <automated>cd /Users/ng/Desktop/fuzzyband/training && .venv/bin/python -m pytest tests/test_dataset_build.py::TestRulePatternForState tests/test_dataset_build.py::TestOracleLabel -v 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - training/tests/test_dataset_build.py contains `class TestRulePatternForState`
    - training/tests/test_dataset_build.py contains `class TestOracleLabel`
    - training/tests/test_dataset_build.py contains `test_gmd_state3_clamped_to_soft`
    - training/tests/test_dataset_build.py contains `test_breakdown_override_fires`
    - training/tests/test_dataset_build.py contains `test_silent_never_overridden`
    - pytest for TestRulePatternForState and TestOracleLabel exits 0 with 9 tests passing (7 + 2)
  </acceptance_criteria>

  <done>All 9 oracle unit tests pass; GMD state=3.0 clamp, Breakdown override, and SILENT guard all have named test coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → Python pipeline | Input MIDI/TFDS files are local, gitignored training data — no external network trust boundary in this plan |
| Python training scripts → output tensors | Scripts write .pt files; no user-facing API surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | _K_SOFT_MID_BPM / _K_SOFT_LOUD_BPM constants | mitigate | Constants documented with source reference (pattern_rules.h line numbers) to prevent silent drift; code review verifies exact values 120.0 and 160.0 |
| T-32-02 | Information Disclosure | training data in gitignored data/ | accept | data/ is gitignored per project config; no PII in MIDI training data |
</threat_model>

<verification>
After both tasks are complete, run the full existing test suite to confirm no regressions:

  cd /Users/ng/Desktop/fuzzyband/training && .venv/bin/python -m pytest tests/test_dataset_build.py -x -v

Expected: all tests pass including the new TestRulePatternForState and TestOracleLabel classes.

Confirm removal of quantile-bin functions:

  grep -c "_activity_score\|_labels_from_train_quantiles" /Users/ng/Desktop/fuzzyband/training/build_dataset.py

Expected: 0
</verification>

<success_criteria>
- training/build_dataset.py assigns Y via _oracle_label (rule oracle + Breakdown heuristic)
- _activity_score and _labels_from_train_quantiles absent from build_dataset.py
- GMD state=3.0 clamped to min(int(state_float), 2) before oracle call
- 9 new unit tests pass (7 for _rule_pattern_for_state, 2 for _oracle_label)
- Full test_dataset_build.py test suite exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/32-training-label-correction/32-01-SUMMARY.md`
</output>
