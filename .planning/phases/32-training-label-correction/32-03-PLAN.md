---
phase: "32"
plan: "03"
type: execute
wave: 2
depends_on:
  - "32-01"
  - "32-02"
files_modified:
  - assets/accompaniment_model.onnx
autonomous: false
requirements:
  - LABEL-03

must_haves:
  truths:
    - "Full pipeline runs without error: build_dataset.py → build_lakh_dataset.py → merge_datasets.py → train_gmd.py"
    - "train_gmd.py macro-F1 gate (>= 0.55) passes on the retrained model"
    - "DATA-06 histogram gate (>= 50 examples per class including class 6) passes in merge_datasets.py"
    - "validate_onnx_contract.py --pattern exits 0 on the retrained artifact"
    - "assets/accompaniment_model.onnx promoted from the retrained artifact"
  artifacts:
    - path: "assets/accompaniment_model.onnx"
      provides: "Retrained pattern model with oracle labels"
      min_lines: 1
    - path: "training/artifacts/"
      provides: "Timestamped training run directory"
      contains: "pattern_trained.onnx"
  key_links:
    - from: "train_gmd.py"
      to: "assets/accompaniment_model.onnx"
      via: "install-model-local.sh --pattern-onnx"
      pattern: "accompaniment_model.onnx"
---

<objective>
Run the corrected data pipeline end-to-end, retrain the pattern model, validate the ONNX contract, and promote the retrained artifact to `assets/accompaniment_model.onnx`.

Purpose: This plan is the execution step that converts the label corrections from Plans 01 and 02 into a shipped model artifact. It is the concrete delivery of LABEL-03.

Output: Retrained `assets/accompaniment_model.onnx` with oracle-labeled training data; pipeline run log confirming DATA-06 and macro-F1 gates pass.
</objective>

<execution_context>
@/Users/ng/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ng/Desktop/fuzzyband/.planning/PROJECT.md
@/Users/ng/Desktop/fuzzyband/.planning/ROADMAP.md
@/Users/ng/Desktop/fuzzyband/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full corrected pipeline (build → merge → train → validate)</name>

  <read_first>
    - training/build_dataset.py (confirm _oracle_label and _K_SOFT_MID_BPM present — Plan 01 changes)
    - training/merge_datasets.py (confirm "Oracle label passthrough" comment present — Plan 02 changes)
    - training/build_lakh_dataset.py (confirm "Y": torch.from_numpy present — Plan 02 changes)
    - scripts/install-model-local.sh (confirm --pattern-onnx and --bass-onnx flags and how to invoke)
    - scripts/validate_onnx_contract.py (confirm --pattern flag behavior and exit codes)
  </read_first>

  <files>assets/accompaniment_model.onnx</files>

  <action>
    Before running anything, verify that Plan 01 and Plan 02 changes are in place:
    - grep for `_oracle_label` in training/build_dataset.py (must exist)
    - grep for `Oracle label passthrough` in training/merge_datasets.py (must exist)
    - grep for `"Y": torch.from_numpy` in training/build_lakh_dataset.py (must exist)

    If any check fails, stop and surface the missing change to the user.

    Run the pipeline in this exact order from the project root:

    Step 1 — Build GMD tensors with oracle labels:
      cd training && .venv/bin/python build_dataset.py --out-dir data/processed

    Watch for:
    - "HISTOGRAM (train split labels)" output with class 6 count
    - "PASS: HISTOGRAM — all classes >= 50 in train split" — if this fails with exit code 3 and class 6 < 50, halt and surface the DATA-06 failure to the user with the actual class 6 count so they can decide to relax the Breakdown threshold (bpm < 120 instead of bpm < 110, or dens < 3.0 instead of dens < 2.5) per D-05 guidance.

    Step 2 — Build Lakh tensors with oracle labels:
      .venv/bin/python build_lakh_dataset.py --out data/lakh/lakh_tensors.pt

    Watch for:
    - "HISTOGRAM (Lakh Y distribution — advisory)" output showing class 6 count (advisory only)

    Step 3 — Merge GMD + Lakh tensors:
      .venv/bin/python merge_datasets.py --out-dir data/processed

    Watch for:
    - "Oracle label passthrough" in output (confirms new code path running)
    - "PASS: HISTOGRAM — all classes >= 50 in merged train" — if this fails, surface class distribution to user.

    Step 4 — Retrain pattern model:
      .venv/bin/python train_gmd.py --data-dir data/processed

    Watch for:
    - "macro-F1" in output — must be >= 0.55 (gate enforced internally)
    - Artifact path logged (e.g., "training/artifacts/pattern-merged-<timestamp>/pattern_trained.onnx")
    - Note the full path to pattern_trained.onnx for Step 5.

    Step 5 — Validate ONNX contract (from project root):
      cd .. && python scripts/validate_onnx_contract.py --pattern training/artifacts/pattern-merged-<timestamp>/pattern_trained.onnx

    Expected: exits 0. If non-zero, surface full output to user.

    Step 6 — Promote to assets/:
      ./scripts/install-model-local.sh \
        --pattern-onnx training/artifacts/pattern-merged-<timestamp>/pattern_trained.onnx \
        --bass-onnx assets/bass_model.onnx

    Note: `install-model-local.sh` requires both pattern and bass ONNX. Pass the EXISTING `assets/bass_model.onnx` as --bass-onnx — the bass model is NOT being retrained in Phase 32. If assets/bass_model.onnx does not exist, halt and surface this to the user (see Pitfall 5 in RESEARCH.md).
  </action>

  <verify>
    <automated>python /Users/ng/Desktop/fuzzyband/scripts/validate_onnx_contract.py --pattern /Users/ng/Desktop/fuzzyband/assets/accompaniment_model.onnx</automated>
  </verify>

  <acceptance_criteria>
    - build_dataset.py exits 0 with "PASS: HISTOGRAM — all classes >= 50 in train split"
    - merge_datasets.py exits 0 with "PASS: HISTOGRAM — all classes >= 50 in merged train"
    - train_gmd.py exits 0 with logged macro-F1 >= 0.55
    - validate_onnx_contract.py --pattern exits 0 on the promoted assets/accompaniment_model.onnx
    - assets/accompaniment_model.onnx exists and has modification time after the start of this plan execution
    - install-model-local.sh reports success (exits 0)
  </acceptance_criteria>

  <done>Full pipeline runs clean; model retrained on oracle labels; DATA-06 and macro-F1 gates pass; ONNX contract validated; assets/accompaniment_model.onnx promoted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify promoted model in DAW or via manual spot-check</name>

  <what-built>Task 1 retrained accompaniment_model.onnx on oracle labels and promoted it to assets/. The model now selects patterns using rule-oracle semantics instead of quantile-bin ranking. This is a semantics change, not a shape change — the ONNX I/O contract is identical to v0.4.</what-built>

  <how-to-verify>
    Option A (plugin smoke — preferred if DAW is available):
    1. Build the plugin with MA_ENABLE_ONNX=ON:
       cmake -B build -DCMAKE_BUILD_TYPE=Release -DMA_ENABLE_ONNX=ON && cmake --build build
    2. Load the rebuilt plugin in Reaper/Logic
    3. Play three distinct intensities (quiet strumming, moderate riff, hard picking)
    4. Confirm the pattern index changes across the three intensities (non-degenerate selection)
    5. Type "approved" if pattern selection is non-degenerate, or describe any issues

    Option B (offline inspection — acceptable if DAW not immediately available):
    1. Run: python scripts/validate_onnx_contract.py --pattern assets/accompaniment_model.onnx
    2. Confirm exit 0 with output showing "[1, 7] float32 → [1] int64" shape contract
    3. Run a quick sanity inference via Python:
       cd training && .venv/bin/python -c "
       import onnxruntime as ort, numpy as np
       s = ort.InferenceSession('../assets/accompaniment_model.onnx')
       # SILENT at 120 bpm
       x = np.array([[120.0, 0.01, 400.0, 0.0, 1.0, 0.0, 0.0]], dtype=np.float32)
       y = s.run(['Y'], {'X': x})[0][0]
       print(f'SILENT input -> pattern {y} (expect 0)')
       # LOUD at 140 bpm
       x = np.array([[140.0, 0.8, 3000.0, 0.5, 0.0, 0.0, 1.0]], dtype=np.float32)
       y = s.run(['Y'], {'X': x})[0][0]
       print(f'LOUD 140bpm input -> pattern {y} (expect 4 or near)')
       "
    4. Type "approved" if the output is plausible, or describe any unexpected values.
  </how-to-verify>

  <resume-signal>Type "approved" to complete Phase 32, or describe any issues with the model output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| training pipeline → assets/accompaniment_model.onnx | Untrusted ONNX artifact written to assets/; validated before promotion |
| plugin (MA_ENABLE_ONNX=ON) → ONNX model | Model loaded by C++ ORT runtime; shape contract validated separately |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Tampering | assets/accompaniment_model.onnx replacement | mitigate | validate_onnx_contract.py --pattern must exit 0 before promotion; install-model-local.sh runs validation atomically |
| T-32-07 | Denial of Service | DATA-06 gate failure blocking pipeline | accept | Gate failure is intentional — surfaces class imbalance for human decision on threshold. Pitfall 4 in RESEARCH.md documents the recovery path. |
| T-32-08 | Elevation of Privilege | bass_model.onnx path in install-model-local.sh | mitigate | Pass existing assets/bass_model.onnx explicitly; no new bass model trained; validator checks both artifacts |
</threat_model>

<verification>
Final state check after checkpoint approved:

1. ONNX contract:
   python /Users/ng/Desktop/fuzzyband/scripts/validate_onnx_contract.py --pattern /Users/ng/Desktop/fuzzyband/assets/accompaniment_model.onnx
   Expected: exits 0

2. Model file exists and was recently modified:
   ls -la /Users/ng/Desktop/fuzzyband/assets/accompaniment_model.onnx
   Expected: file present, mtime from this phase execution

3. All three pipeline scripts have oracle labeling:
   grep -l "_oracle_label" /Users/ng/Desktop/fuzzyband/training/build_dataset.py /Users/ng/Desktop/fuzzyband/training/build_lakh_dataset.py
   Expected: both files listed

4. merge_datasets.py has no quantile code:
   grep -v "^#" /Users/ng/Desktop/fuzzyband/training/merge_datasets.py | grep -c "searchsorted\|qt = np.quantile"
   Expected: 0
</verification>

<success_criteria>
- Full pipeline runs without error on oracle-labeled data
- DATA-06 histogram gate passes (>= 50 examples per class including class 6) in both build_dataset.py and merge_datasets.py
- train_gmd.py macro-F1 >= 0.55 gate passes
- scripts/validate_onnx_contract.py --pattern exits 0 on the promoted artifact
- assets/accompaniment_model.onnx updated with retrained oracle-labeled model
- Human checkpoint confirms non-degenerate pattern selection or ONNX contract spot-check passes
</success_criteria>

<output>
After completion, create `.planning/phases/32-training-label-correction/32-03-SUMMARY.md`
</output>
