---
phase: 24-ui-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analysis/FeatureVector.h
  - src/AccompanimentProcessor.cpp
  - src/AccompanimentEditor.h
  - src/AccompanimentEditor.cpp
  - src/inference/RuleBasedInference.cpp
  - src/inference/OnnxInference.cpp
  - src/inference/PolicyPatternMapper.h
  - src/inference/PolicyPatternMapper.cpp
  - CMakeLists.txt
  - tests/test_rule_based_inference.cpp
  - tests/test_processor_pipeline.cpp
  - tests/PolicyPatternMapperTests.cpp
  - tests/test_policy_pattern_mapper_full.cpp
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Plugin builds cleanly (Release + Debug) with genre and variation parameters absent"
    - "No reference to PolicyPatternMapper exists in any compiled source file"
    - "policyVariation field absent from FeatureVector struct"
    - "Editor shows exactly 3 controls: Intensity, structureBlend, Generative bass — no Genre or Variation widgets"
    - "Section heading reads 'Controls' not 'style'"
    - "All existing unit tests still pass after removals"
    - "RuleBasedInference uses std::clamp(base, 0, 6) directly — no intermediate namespace"
  artifacts:
    - path: "src/analysis/FeatureVector.h"
      provides: "FeatureVector struct without policyVariation"
      contains: "policyIntensity"
    - path: "src/inference/RuleBasedInference.cpp"
      provides: "Direct clamp, no PolicyPatternMapper"
    - path: "src/AccompanimentEditor.cpp"
      provides: "3-control layout, 'Controls' heading"
  key_links:
    - from: "src/inference/RuleBasedInference.cpp"
      to: "std::clamp"
      via: "#include <algorithm>"
      pattern: "std::clamp"
    - from: "src/AccompanimentEditor.cpp"
      to: "userPolicyHeading"
      via: "setText"
      pattern: "Controls"
---

<objective>
Atomically remove genre APVTS parameter, variation APVTS parameter, PolicyPatternMapper namespace, policyVariation FeatureVector field, and all editor widgets for genre and variation. Update resized() to distribute the reclaimed vertical space across the 3 remaining controls. Update tests to remove references to deleted components. All changes must land in one commit.

Purpose: UI-01 and UI-02 require all four affected files to change together — partial removal would crash on old session load due to parameter state deserialization mismatch.

Output: Clean build with genre/variation/PolicyPatternMapper gone; 3-control editor; updated test suite passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-ui-simplification/24-CONTEXT.md
@.planning/phases/24-ui-simplification/24-UI-SPEC.md

<interfaces>
<!-- Key facts for executor — no codebase exploration needed -->

From src/analysis/FeatureVector.h (CURRENT — to be modified):
```cpp
struct FeatureVector
{
    float bpm = 120.0f;
    float rmsEnergy = 0.0f;
    float spectralCentroid = 0.0f;
    float highFreqFlux = 0.0f;
    StructureState state = StructureState::SILENT;
    int64_t sampleTimestamp = 0;
    float pitchRootMidi = 40.0f;
    float pitchConfidence = 0.0f;
    float policyIntensity = 0.5f;
    float policyVariation = 0.5f;   // ← REMOVE THIS FIELD ONLY
};
```

From src/inference/RuleBasedInference.cpp (CURRENT):
- Line 2: `#include "PolicyPatternMapper.h"` — REMOVE
- Line 37: `int result = PolicyPatternMapper::applyPatternPolicy(base, f);` — REPLACE with `int result = std::clamp(base, 0, 6);`
- Add `#include <algorithm>` at line 1 if not already present (needed for std::clamp)

From src/inference/OnnxInference.cpp (CURRENT, inside #if MA_ENABLE_ONNX):
- Line 6: `#include "PolicyPatternMapper.h"` — REMOVE (inside the #if block)
- Line ~151: `int result = PolicyPatternMapper::applyPatternPolicy(clamped, f);` — REPLACE with `int result = clamped;`
  (clamped is already std::clamp(rawIdx, 0, 6) on the preceding line — no further clamping needed)

Window size: 520×600 (setSize(520, 600) in AccompanimentEditor constructor — confirmed by 24-UI-SPEC.md)

Target resized() control section (from 24-UI-SPEC.md):
  userPolicyHeading.setBounds(r.removeFromTop(20));
  r.removeFromTop(8);
  row = r.removeFromTop(52);
  intensityLabel.setBounds(row.removeFromLeft(140));
  intensitySlider.setBounds(row);
  r.removeFromTop(8);
  row = r.removeFromTop(52);
  structureBlendLabel.setBounds(row.removeFromLeft(140));
  structureBlendSlider.setBounds(row);
  r.removeFromTop(8);
  row = r.removeFromTop(60);
  generativeBassLabel.setBounds(row.removeFromLeft(140));
  generativeBassModeCombo.setBounds(row);

Label loop after removal (AccompanimentEditor.cpp line 46):
  for (auto* l : { &intensityLabel, &structureBlendLabel, &generativeBassLabel })

CMakeLists.txt lines to remove (from plugin SOURCES list and test source lists):
  - Line 149: src/inference/PolicyPatternMapper.cpp
  - Line 260: tests/PolicyPatternMapperTests.cpp
  - Line 267: tests/test_policy_pattern_mapper_full.cpp
  - Line 278: src/inference/PolicyPatternMapper.cpp (from test sources)

tests/test_rule_based_inference.cpp line 37:
  f.policyVariation = 0.5f;   // ← REMOVE THIS LINE ONLY
  (f.policyIntensity = 0.5f; on the line before it STAYS)

tests/test_processor_pipeline.cpp changes:
  - Lines 147-189 (round-trip test): Remove genre and variation parameter blocks entirely.
    Keep the test but only test intensity and structureBlend:
      set intensity to 0.8f, assert intensity survives round-trip within 0.01f tolerance.
      Remove: genre setValue(0.75f), variation setValue(0.2f), and their corresponding
      REQUIRE assertions on procB.
  - Lines 192-223 (genre pattern shift test): DELETE ENTIRE TEST CASE
    ("Processor pipeline: changing genre shifts displayPatternIndex via PolicyPatternMapper")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove policyVariation from FeatureVector and delete PolicyPatternMapper files</name>
  <files>
    src/analysis/FeatureVector.h,
    src/inference/PolicyPatternMapper.h,
    src/inference/PolicyPatternMapper.cpp,
    src/inference/RuleBasedInference.cpp,
    src/inference/OnnxInference.cpp,
    CMakeLists.txt
  </files>

  <read_first>
    - src/analysis/FeatureVector.h (full file — confirm policyVariation is the last field)
    - src/inference/RuleBasedInference.cpp (find #include "PolicyPatternMapper.h" and the applyPatternPolicy call)
    - src/inference/OnnxInference.cpp (find both PolicyPatternMapper references — one in #if block header, one in selectPattern)
    - CMakeLists.txt lines 145–155 (plugin SOURCES list) and lines 255–285 (test source lists)
  </read_first>

  <action>
**Step 1 — FeatureVector.h:** Remove the line `float policyVariation = 0.5f;` from the struct. The line `float policyIntensity = 0.5f;` immediately above it STAYS. The struct must not have policyVariation after this change.

**Step 2 — Delete PolicyPatternMapper files:** Delete `src/inference/PolicyPatternMapper.h` and `src/inference/PolicyPatternMapper.cpp` using file removal (git rm or rm).

**Step 3 — RuleBasedInference.cpp:** 
  a. Remove the line `#include "PolicyPatternMapper.h"` from the includes section.
  b. Confirm `#include <algorithm>` is present (needed for std::clamp). If absent, add it at the top of the includes.
  c. Replace the line `int result = PolicyPatternMapper::applyPatternPolicy(base, f);` with `int result = std::clamp(base, 0, 6);`
     Note: The parameter name in context is `base` — verify the actual local variable name by reading the function before editing.

**Step 4 — OnnxInference.cpp:**
  a. Remove `#include "PolicyPatternMapper.h"` from inside the `#if MA_ENABLE_ONNX` include block.
  b. In `selectPattern()` inside the `#if MA_ENABLE_ONNX` block, find the line calling `PolicyPatternMapper::applyPatternPolicy(clamped, f)` and replace it with `int result = clamped;`
     (The variable `clamped` is already the result of `std::clamp(rawIdx, 0, 6)` on the preceding line — no additional clamping is needed.)

**Step 5 — CMakeLists.txt:** Remove exactly these 4 lines (verify line numbers by reading before editing):
  - `src/inference/PolicyPatternMapper.cpp` from the MetalAccompaniment plugin SOURCES list
  - `tests/PolicyPatternMapperTests.cpp` from the MetalAccompanimentTests SOURCES list
  - `tests/test_policy_pattern_mapper_full.cpp` from the MetalAccompanimentTests SOURCES list
  - The second `src/inference/PolicyPatternMapper.cpp` entry from the MetalAccompanimentTests SOURCES list (test executable also compiled the cpp)
  </action>

  <verify>
    <automated>grep -r "PolicyPatternMapper" /Users/ng/Desktop/fuzzyband/src/ /Users/ng/Desktop/fuzzyband/CMakeLists.txt 2>/dev/null | grep -v '^Binary' | wc -l | xargs -I{} sh -c 'test {} -eq 0 &amp;&amp; echo PASS || echo FAIL'</automated>
  </verify>

  <done>
    - `src/inference/PolicyPatternMapper.h` and `.cpp` do not exist on disk
    - No remaining reference to `PolicyPatternMapper` in src/ or CMakeLists.txt
    - `FeatureVector.h` contains `policyIntensity` but not `policyVariation`
    - `RuleBasedInference.cpp` contains `std::clamp` and does not include `PolicyPatternMapper.h`
    - `OnnxInference.cpp` does not include `PolicyPatternMapper.h`
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove genre/variation APVTS params, editor widgets, update resized(), update tests — atomic commit</name>
  <files>
    src/AccompanimentProcessor.cpp,
    src/AccompanimentEditor.h,
    src/AccompanimentEditor.cpp,
    tests/test_rule_based_inference.cpp,
    tests/test_processor_pipeline.cpp,
    tests/PolicyPatternMapperTests.cpp,
    tests/test_policy_pattern_mapper_full.cpp
  </files>

  <read_first>
    - src/AccompanimentProcessor.cpp lines 53–105 (createParameterLayout) and lines 460–475 (policyVariation write in processBlock)
    - src/AccompanimentEditor.h (member declarations for genreLabel, genreCombo, variationLabel, variationSlider, genreAttachment, variationAttachment)
    - src/AccompanimentEditor.cpp lines 40–60 (constructor body: heading setText, label loop, widget adds) and lines 155–220 (resized())
    - tests/test_rule_based_inference.cpp lines 30–60 (exclusion test with policyVariation)
    - tests/test_processor_pipeline.cpp lines 145–225 (round-trip test + genre pattern test)
  </read_first>

  <action>
**Step 1 — AccompanimentProcessor.cpp createParameterLayout():**
  Remove the entire genre AudioParameterChoice block (the juce::StringArray genreChoices lines + layout.add for "genre").
  Remove the entire variation AudioParameterFloat block (layout.add for "variation").
  The intensity, structureBlend, and generativeBassMode parameters STAY.

**Step 2 — AccompanimentProcessor.cpp processBlock():**
  Remove the two lines that read the "variation" APVTS parameter and write to fv.policyVariation:
  ```cpp
  if (auto* rawVariation = apvts.getRawParameterValue("variation"))
      fv.policyVariation = rawVariation->load();
  ```
  The intensity read (rawIntensity → fv.policyIntensity) STAYS unchanged.

**Step 3 — AccompanimentEditor.h member removals:**
  Remove these 6 member declarations from the class:
  - `juce::Label genreLabel{ {}, "Genre" };`
  - `juce::ComboBox genreCombo;`
  - `juce::Label variationLabel{ {}, "Variation" };`
  - `juce::Slider variationSlider;`
  - `std::unique_ptr<juce::AudioProcessorValueTreeState::ComboBoxAttachment> genreAttachment;`
  - `std::unique_ptr<juce::AudioProcessorValueTreeState::SliderAttachment> variationAttachment;`

**Step 4 — AccompanimentEditor.cpp constructor body:**
  a. Change `userPolicyHeading.setText("style", juce::dontSendNotification);` to `userPolicyHeading.setText("Controls", juce::dontSendNotification);`
  
  b. Update the label styling loop to remove genreLabel and variationLabel — the loop must be exactly:
     ```cpp
     for (auto* l : { &intensityLabel, &structureBlendLabel, &generativeBassLabel })
     ```
  
  c. Remove the genreCombo initialization block entirely:
     ```cpp
     genreCombo.addItem("Metal",      1);
     genreCombo.addItem("Metalcore",  2);
     genreCombo.addItem("Death",      3);
     genreCombo.addItem("Progressive",4);
     genreCombo.addItem("Black",      5);
     genreCombo.setTooltip("Style preset for pattern policy (saved with project).");
     addAndMakeVisible(genreLabel);
     addAndMakeVisible(genreCombo);
     ```
  
  d. Remove the variationSlider initialization block entirely:
     ```cpp
     variationSlider.setSliderStyle(juce::Slider::LinearHorizontal);
     variationSlider.setTextBoxStyle(juce::Slider::TextBoxBelow, false, 72, 18);
     variationSlider.setRange(0.0, 1.0, 0.001);
     addAndMakeVisible(variationLabel);
     addAndMakeVisible(variationSlider);
     ```
  
  e. Remove the genreAttachment and variationAttachment initialization lines:
     ```cpp
     genreAttachment = std::make_unique<juce::AudioProcessorValueTreeState::ComboBoxAttachment>(
         apvts, "genre", genreCombo);
     variationAttachment = std::make_unique<juce::AudioProcessorValueTreeState::SliderAttachment>(
         apvts, "variation", variationSlider);
     ```

**Step 5 — AccompanimentEditor.cpp resized():**
  In the control panel section of resized(), remove the genre row block and the variation row block, then expand the 3 remaining rows to the new heights per 24-UI-SPEC.md. The exact code to write for the control section is:

  ```cpp
  userPolicyHeading.setBounds(r.removeFromTop(20));
  r.removeFromTop(8);

  row = r.removeFromTop(52);
  intensityLabel.setBounds(row.removeFromLeft(140));
  intensitySlider.setBounds(row);
  r.removeFromTop(8);

  row = r.removeFromTop(52);
  structureBlendLabel.setBounds(row.removeFromLeft(140));
  structureBlendSlider.setBounds(row);
  r.removeFromTop(8);

  row = r.removeFromTop(60);
  generativeBassLabel.setBounds(row.removeFromLeft(140));
  generativeBassModeCombo.setBounds(row);
  ```

  Everything before this section (titleRow, gaps) and after (post-panel gap, diagnostic labels, button, helpCaption) is UNCHANGED.

**Step 6 — tests/test_rule_based_inference.cpp:**
  Remove the single line `f.policyVariation = 0.5f;` at line 37 (inside the exclusion test).
  The line `f.policyIntensity = 0.5f;` immediately before it STAYS.

**Step 7 — tests/test_processor_pipeline.cpp:**
  a. In the round-trip test (lines ~147–189): Replace the entire test body to remove genre and variation. The updated test must:
     - Set `intensity` to 0.8f via `p->setValue(0.8f)` only (remove genre and variation setValue calls)
     - Call `getStateInformation` and `setStateInformation` as before
     - Assert only that intensity survives: `REQUIRE(std::abs(inten->get() - 0.8f) < 0.01f)`
     - Remove the genre getIndex() REQUIRE and the variation REQUIRE entirely
     - Keep the `data.getSize() > 0` assertion
  
  b. Delete the entire TEST_CASE block "Processor pipeline: changing genre shifts displayPatternIndex via PolicyPatternMapper" (lines ~192–223) — remove from the opening `TEST_CASE(` line through the closing `}` of that test.

**Step 8 — Delete test files for PolicyPatternMapper:**
  Delete `tests/PolicyPatternMapperTests.cpp` and `tests/test_policy_pattern_mapper_full.cpp` (these were already removed from CMakeLists.txt in Task 1 — now remove the actual files).

**Step 9 — Atomic commit:**
  Stage ALL modified and deleted files together in a single commit:
  ```
  git add src/analysis/FeatureVector.h
  git add src/AccompanimentProcessor.cpp
  git add src/AccompanimentEditor.h
  git add src/AccompanimentEditor.cpp
  git add src/inference/RuleBasedInference.cpp
  git add src/inference/OnnxInference.cpp
  git rm src/inference/PolicyPatternMapper.h
  git rm src/inference/PolicyPatternMapper.cpp
  git add CMakeLists.txt
  git add tests/test_rule_based_inference.cpp
  git add tests/test_processor_pipeline.cpp
  git rm tests/PolicyPatternMapperTests.cpp
  git rm tests/test_policy_pattern_mapper_full.cpp
  ```
  Commit message: `feat(24-01): remove genre/variation/PolicyPatternMapper atomically (UI-01, UI-02)`
  </action>

  <verify>
    <automated>cd /Users/ng/Desktop/fuzzyband && cmake -B build -DCMAKE_BUILD_TYPE=Release -DMA_BUILD_TESTS=ON -DMA_ENABLE_ONNX=OFF 2>&amp;1 | tail -5 &amp;&amp; cmake --build build --target MetalAccompanimentTests -j4 2>&amp;1 | tail -20 &amp;&amp; ./build/MetalAccompanimentTests_artefacts/Release/MetalAccompanimentTests 2>&amp;1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `grep -r "genre" src/AccompanimentEditor.h src/AccompanimentEditor.cpp src/AccompanimentProcessor.cpp` returns 0 matches
    - `grep -r "variation" src/AccompanimentEditor.h src/AccompanimentEditor.cpp src/AccompanimentProcessor.cpp` returns 0 matches (policyVariation gone, variation parameter gone)
    - `grep "policyVariation" src/analysis/FeatureVector.h` returns 0 matches
    - `grep "Controls" src/AccompanimentEditor.cpp` returns at least 1 match (heading text)
    - `grep "std::clamp" src/inference/RuleBasedInference.cpp` returns at least 1 match
    - `grep "PolicyPatternMapper" src/inference/OnnxInference.cpp` returns 0 matches
    - `test -f src/inference/PolicyPatternMapper.h` exits non-zero (file does not exist)
    - `test -f src/inference/PolicyPatternMapper.cpp` exits non-zero (file does not exist)
    - Build succeeds with `MA_ENABLE_ONNX=OFF`
    - All Catch2 tests pass (MetalAccompanimentTests executable exits 0)
    - Git log shows exactly one new commit staging all the above files together
  </acceptance_criteria>

  <done>
    Genre and variation are absent from the plugin binary. PolicyPatternMapper is gone. policyVariation field is gone. The editor shows Controls / Intensity / structureBlend / Generative bass only with expanded row heights. Test suite passes. All changes land in one atomic commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| APVTS deserialization | External session XML (from DAW or file) enters setStateInformation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | APVTS XML deserialization | accept | JUCE silently ignores unknown XML attributes — no special handling needed; confirmed by CONTEXT.md code insights. The round-trip test in Plan 24-02 gates this. |
| T-24-02 | Denial of Service | Genre param lookup in old session | mitigate | Remove genre from createParameterLayout() — JUCE won't register unknown params as valid, avoiding any null-deref on getParameter("genre") in new code. |
</threat_model>

<verification>
After both tasks complete and the commit is made:
- `git show --stat HEAD` shows all atomic-commit files in one commit
- `grep -r "PolicyPatternMapper" src/ tests/ CMakeLists.txt` returns zero results
- `grep -r "policyVariation" src/ tests/` returns zero results
- `grep -r '"genre"' src/` returns zero results
- `grep -r '"variation"' src/` returns zero results
- Build in Release with MA_ENABLE_ONNX=OFF succeeds without errors or warnings about removed identifiers
- All Catch2 tests pass
</verification>

<success_criteria>
- genre APVTS parameter absent from createParameterLayout()
- variation APVTS parameter absent from createParameterLayout()
- PolicyPatternMapper.h and PolicyPatternMapper.cpp deleted from disk
- policyVariation removed from FeatureVector struct
- All editor genre/variation members, widgets, and attachments removed
- Section heading changed from "style" to "Controls"
- resized() control section uses 52px/52px/60px row heights for the 3 remaining controls
- CMakeLists.txt has no PolicyPatternMapper.cpp entries
- Tests referencing deleted components updated or removed
- All changes in one atomic git commit
- MetalAccompanimentTests passes (cmake --build + test runner exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/24-ui-simplification/24-01-SUMMARY.md` with:
- What was removed (list all files changed/deleted)
- The exact commit hash
- Confirmation that test suite passes
- Any deviations from the plan (e.g. different line numbers found vs expected)
</output>
