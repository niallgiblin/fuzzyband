# Copy to ../backend.hcl (gitignored) and fill from scripts/bootstrap-tfstate.sh output.

bucket         = "YOUR_TFSTATE_BUCKET"
key            = "fuzzyband/phase16/terraform.tfstate"
region         = "us-east-1"
dynamodb_table = "YOUR_LOCK_TABLE"
encrypt        = true
